import { CompanyInformation } from './company-information';

describe('CompanyInformation', () => {
  it('should create an instance', () => {
    expect(new CompanyInformation()).toBeTruthy();
  });
});
